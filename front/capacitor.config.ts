import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ruitza.LifeAgricultureApp',
  appName: 'LifeAgricultureApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
